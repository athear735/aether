# AETHER Streamlit Secrets Configuration
# Copy this to secrets.toml and fill in your values
# DO NOT commit secrets.toml to version control!

# API Configuration
API_URL = "https://your-api-backend.com"  # Your deployed API endpoint
# Or use a local endpoint for testing
# API_URL = "http://localhost:8000"

# Optional: OpenAI API Key (if using OpenAI models)
# OPENAI_API_KEY = "sk-..."

# Optional: Other API keys
# HUGGINGFACE_API_KEY = "hf_..."

# Database (if needed)
# DATABASE_URL = "postgresql://..."

# Authentication (if implementing user auth)
# SECRET_KEY = "your-secret-key-here"
# ALGORITHM = "HS256"
# ACCESS_TOKEN_EXPIRE_MINUTES = 30
